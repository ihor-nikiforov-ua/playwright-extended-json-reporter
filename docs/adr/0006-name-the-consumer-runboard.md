# Name the consumer Runboard

The downstream dashboard product is named **Runboard**, and the Playwright reporter package and GitHub repository should be named `playwright-runboard-reporter`. We chose a consumer-led name over a generic `playwright-*-reporter` name because established dashboard ecosystems name their Playwright reporters after the destination product, and because `playwright-dashboard-reporter` was already taken on npm.
